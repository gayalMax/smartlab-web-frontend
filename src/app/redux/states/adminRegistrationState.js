/**
 * Initial state of the admin registration state
 */
const initialState = {
  action: null,
  tokens: [],
  roles: [],
  error: null,
  loading: false,
  success: false
};

export default initialState;
